{
  "name": "google-ads-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered Google Ads automation for marketing agencies (MVP)",
  "scripts": {
    "dev": "pnpm --filter @gaa/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.28.1"
}
