// Empty stub for the `server-only` marker package under vitest. The real
// package throws when bundled outside a React Server Component; in node-env unit
// tests we just need the import to resolve to a no-op. Aliased in vitest.config.ts.
export {};
